# Minimal requirements for Dota 2 Auto Accept
# Only includes packages that are actually imported and used

# Core GUI and system interaction
pyautogui==0.9.54
pygetwindow==0.0.9
mss==10.0.0

# Image processing
opencv-python-headless==4.11.0.86  # headless version is smaller, no GUI components
pillow==11.2.1
numpy==1.26.4
scikit-image==0.25.2

# Audio
pygame==2.6.1
sounddevice==0.5.2

# Build tool
pyinstaller==6.12.0
pyinstaller-hooks-contrib==2025.2
